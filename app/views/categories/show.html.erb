<h1>Articles About <%= @category.name %></h1>
<% if logged_in? && current_user.admin? %>
  <%= link_to "Rename category", edit_category_path, class: "action_button action_button-yellow"%>
<% end %>

<%= render 'articles/article', obj: @category_articles%>
